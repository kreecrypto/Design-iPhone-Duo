name: App icons overlay browser QA

on:
  push:
    branches: [main]
    paths:
      - index.html
      - tests/app-icons.browser.test.mjs
      - .github/workflows/app-icons.yml
  pull_request:
    paths:
      - index.html
      - tests/app-icons.browser.test.mjs
      - .github/workflows/app-icons.yml

permissions:
  contents: read

jobs:
  app-icons-overlay:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Verify Inner app icons toggle and state preservation
        run: node tests/app-icons.browser.test.mjs
